# UPSTREAM — mission-readiness-framer

**Origin:** Theseus / govcon-capture-vibe — original skill (Jun 2026).

**License:** MIT (see SKILL.md frontmatter).

**Replaces:** `rfp-reverse-engineer` for Intel Briefings customer-intent and capture strategy handoff. The deprecated skill retains CO stance-inversion / trap forensics for legacy runs.

**Contract:** Emits `artifacts/mission_readiness_frame.json` + `artifacts/brief.md`. Produces `strategy_handoff` / `mission_readiness_handoff` for `proposal-generator` chain contracts.