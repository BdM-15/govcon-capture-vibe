
!inputs/.gitkeep
!logs/.gitkeep
!rag_storage/.gitkeep
# ============================================================================
# API key files
# API Keys and Secrets
# Archived documentation (preserved in git)
# Build and Distribution
# by both the WebUI upload endpoints and the /scan-rfp filesystem flow.
# C extensions
# Compiled Python
# Cursor (agent scratch/analysis files)
# Database files
# Dep snapshots (Phase 3a backups)
# docs/archive/* intentionally tracked
# Documentation and Examples
# E2E harness run logs (developer-machine only)
# Emacs
# Environment files (CRITICAL - never commit)
# External Analysis (Navy MBOS baseline, etc.)
# Graphify generated graph/wiki output
# IDEs and Editors
# Jupyter Notebooks
# Keep empty folders for downstream tooling
# Knowledge graph storage (regenerated from RFPs)
# Large files that shouldn't be tracked
# Linux
# Log files (not used by current code, regenerated if needed)
# macOS
# Memory dumps
# Miscellaneous
# Node (huashu-design renderer toolchain)
# Operating Systems
# Project-Specific: Input Documents
# Project-Specific: RAG Storage and Processing
# PyCharm / IntelliJ
# PyInstaller
# Python
# RFP source documents are staged per-workspace under inputs/<workspace>/
# Skill snapshots (skill-creator pre-edit backups)
# Stray UI test response artifacts (loose at repo root)
# Sublime Text
# Temporary processing files
# Theseus local artifacts
# They may contain sensitive/proprietary data — never commit.
# Unit test / coverage
# User may save analysis results externally
# UV lock file (tracked, but may be regenerated)
# uv.lock is tracked for reproducibility
# Vim
# Virtual Environments
# VSCode
# Windows
$RECYCLE.BIN/
*$py.class
*.7z
*.backup
*.bak
*.cache
*.code-workspace
*.cover
*.db
*.dll
*.dmp
*.dump
*.dylib
*.egg
*.egg-info/
*.iml
*.ipr
*.ipynb_checkpoints
*.iso
*.iws
*.key
*.manifest
*.p12
*.pem
*.pfx
*.py,cover
*.py[cod]
*.pyc
*.pyd
*.pyo
*.rar
*.so
*.spec
*.sqlite
*.sqlite3
*.sublime-project
*.sublime-workspace
*.swo
*.swp
*.tar.gz
*.temp
*.tmp
*.zip
*~
.\#*
._*
.AppleDouble
.cache
.coverage
.coverage.*
.cursor/
.directory
.DS_Store
.eggs/
.env
.env*
.env.*.local
.env.development
.env.local
.env.production
.env.test
.github/skills/huashu-design/node_modules/
.hypothesis/
.idea/
.installed.cfg
.ipynb_checkpoints/
.LSOverride
.netrwhist
.nox/
.pytest_cache/
.Python
.tox/
.Trash-*
.venv/
.vim/
.vscode/
/response.json
/skill_invocation_response.json
/skill_response.json
\#*\#
__pycache__/
build/
coverage.xml
credentials/
deps-backup-*/
Desktop.ini
develop-eggs/
dist/
docs/Clean_Prompt_Library.xlsx
downloads/
eggs/
ehthumbs.db
ehthumbs_vista.db
env.bak/
env/
external_analysis/
graphify-out/
htmlcov/
inputs/*/**
lib/
lib64/
logs/*.log
logs/*.log.*
MANIFEST
navy_mbos_baseline/
node_modules/
nosetests.xml
package-lock.json
parts/
pip-wheel-metadata/
pythonenv*/
rag_storage/
rag_storage_*/
rfp_analysis_*/
sdist/
secrets/
share/python-wheels/
Thumbs.db
Thumbs.db:encryptable
tools/_dep_snapshots/
tools/_e2e_run_*.log
tools/_e2e_runs/
tools/_prompt_xlsx_dump.json
tools/_skill_snapshots/
var/
venv.bak/
venv/
wheels/
